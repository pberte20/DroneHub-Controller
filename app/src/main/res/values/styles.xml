<resources>
    <!-- Base application theme. -->
    <style name="Theme.HERDController" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="buttonStyle">@style/DefaultButtonStyle</item>
    </style>

    <style name="DefaultButtonStyle" parent="Widget.AppCompat.Button">
        <item name="backgroundTint">@color/colorPrimary</item> <!-- Button background color -->
        <item name="android:textColor">@color/colorWhite</item> <!-- Button text color -->
        <item name="android:background">@drawable/rounded_button_background</item>
    </style>

</resources>
